# SA Cook Assistant Windows Preview v5.51

- Fixes the Windows updater error where GitHub returns HTTP 200 but Electron does not expose a stream body.
- Adds a safe fallback download path using the response array buffer.
- Keeps the faster AUTO timing and independent-question splitting from the previous local build.
