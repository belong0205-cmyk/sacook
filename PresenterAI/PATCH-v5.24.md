# Presenter AI v5.24

- Shrinks the oversized current-answer area so the previous exchange remains visible at the same time.
- Shows `CURRENT` in each independent AUTO and SPACE column.
- Replaces the generic Conversation section with a larger, always-open `Previous` panel.
- Displays both the previous question (`THEM`) and previous answer (`ME`) together for faster scanning.
- Keeps AUTO and SPACE recognition and answer state completely independent.
