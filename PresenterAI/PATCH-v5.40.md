# SA Cook Assistant 5.40

- Changes the default background from extremely transparent to a balanced reading level.
- Adds three instant background choices under `••• → Độ nền`: `Trong – thấy nền rõ`, `Cân bằng`, and `Đậm – chữ rõ hơn`.
- Saves the selected level for the next launch, so transparency no longer requires another app update.
- Keeps the true clear-window implementation without restoring the dark macOS blur material.
